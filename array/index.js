// array = a variable like structure that can hold more than 1 value

let fruit = 'apple';

let frutas = ['apple', 'orange', 'banana'];

console.log (frutas[0]);

frutas.push('coconut');

frutas.unshift('mango');

frutas.shift(); // para poner elementos al inicio del array

